<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blaze Intelligence - Elite Sports Analytics Platform</title>
    <meta name="description" content="Where Texas Grit Meets Silicon Valley Innovation. Pioneer sports analytics through cutting-edge AI, real-time performance tracking, and predictive modeling.">
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto+Mono:wght@400;700&display=swap" rel="stylesheet">
    
    <!-- Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        :root {
            /* Brand Colors - Texas Heritage meets Modern Tech */
            --burnt-orange: #BF5700;
            --cardinal-blue: #9BCBEB;
            --tennessee-deep: #002244;
            --vancouver-teal: #00B2A9;
            --platinum: #E5E4E2;
            --graphite: #36454F;
            --pearl: #F7F9FC; /* Slightly cooler background */
            
            /* Functional Colors */
            --success: #10B981;
            --warning: #F59E0B;
            --error: #EF4444;
            --info: var(--cardinal-blue);
            
            /* Typography */
            --font-display: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            --font-body: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            --font-mono: 'Roboto Mono', monospace;
            
            /* Animations */
            --transition: cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: var(--font-body);
            line-height: 1.6;
            color: var(--graphite);
            background: var(--pearl);
            overflow-x: hidden;
        }

        /* Navigation */
        nav {
            position: fixed;
            top: 0;
            width: 100%;
            background: rgba(247, 249, 252, 0.8);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            z-index: 1000;
            border-bottom: 1px solid rgba(0, 34, 68, 0.1);
            transition: all 0.3s var(--transition);
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            display: flex;
            align-items: center;
            font-weight: 900;
            font-size: 1.5rem;
            color: var(--burnt-orange);
            text-decoration: none;
            transition: transform 0.3s var(--transition);
        }

        .logo:hover {
            transform: scale(1.05);
        }

        .logo-icon {
            width: 40px;
            height: 40px;
            margin-right: 12px;
            background: linear-gradient(135deg, var(--burnt-orange) 0%, var(--tennessee-deep) 100%);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            align-items: center;
        }

        .nav-link {
            color: var(--graphite);
            text-decoration: none;
            font-weight: 500;
            transition: color 0.3s var(--transition);
            position: relative;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--burnt-orange);
            transition: width 0.3s var(--transition);
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .nav-link:hover {
            color: var(--burnt-orange);
        }

        .nav-cta {
            background: var(--burnt-orange);
            color: white;
            padding: 0.75rem 1.5rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s var(--transition);
            border: 2px solid var(--burnt-orange);
        }

        .nav-cta:hover {
            background: transparent;
            color: var(--burnt-orange);
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(191, 87, 0, 0.2);
        }

        /* Hero Section */
        .hero {
            margin-top: 80px; /* nav height */
            height: 90vh;
            min-height: 700px;
            display: flex;
            align-items: center;
            padding: 0 2rem;
            background-color: #000a14;
            position: relative;
            overflow: hidden;
        }

        #hero-canvas {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 1;
        }

        .hero-container {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            align-items: center;
            position: relative;
            z-index: 2;
            width: 100%;
        }

        .hero-content h1 {
            font-size: 3.8rem;
            font-weight: 900;
            line-height: 1.1;
            margin-bottom: 1.5rem;
            color: white;
            text-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
        }

        .tagline {
            font-size: 1.5rem;
            color: var(--cardinal-blue);
            margin-bottom: 1rem;
            font-weight: 600;
        }

        .hero-description {
            font-size: 1.125rem;
            color: var(--platinum);
            margin-bottom: 2rem;
            line-height: 1.8;
            max-width: 550px;
        }

        .hero-cta-group {
            display: flex;
            gap: 1rem;
            flex-wrap: wrap;
        }

        .btn-primary {
            background: var(--burnt-orange);
            color: white;
            padding: 1rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s var(--transition);
            border: 2px solid var(--burnt-orange);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 30px rgba(191, 87, 0, 0.3);
        }

        .btn-secondary {
            background: rgba(255, 255, 255, 0.1);
            color: white;
            padding: 1rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s var(--transition);
            border: 2px solid rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(5px);
        }

        .btn-secondary:hover {
            background: white;
            color: var(--tennessee-deep);
            transform: translateY(-2px);
        }

        /* Live Stats Widget - Glassmorphism style */
        .hero-visual {
            position: relative;
        }

        .live-stats-card {
            background: rgba(255, 255, 255, 0.1);
            border-radius: 16px;
            padding: 2rem;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.2);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
        }

        .live-badge {
            position: absolute;
            top: 1rem;
            right: 1rem;
            background: var(--error);
            color: white;
            padding: 0.25rem 0.75rem;
            border-radius: 20px;
            font-size: 0.75rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(239, 68, 68, 0.7); }
            50% { transform: scale(1.05); box-shadow: 0 0 10px 5px rgba(239, 68, 68, 0); }
        }
        
        .live-stats-card h3 {
             color: white;
        }

        .stat-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 1.5rem;
            margin-top: 2rem;
        }

        .stat-item {
            padding: 1rem;
            background: rgba(0, 0, 0, 0.2);
            border-radius: 12px;
        }

        .stat-label {
            font-size: 0.875rem;
            color: var(--platinum);
            margin-bottom: 0.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .stat-value {
            font-size: 2rem;
            font-weight: 700;
            color: var(--burnt-orange);
            font-family: var(--font-mono);
        }

        .stat-change {
            font-size: 0.75rem;
            color: var(--success);
            margin-top: 0.25rem;
        }

        /* General Section Styling */
        .section {
            padding: 6rem 2rem;
        }

        .section-header {
            text-align: center;
            max-width: 800px;
            margin: 0 auto 4rem;
        }

        .section-header h2 {
            font-size: 3rem;
            font-weight: 900;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, var(--burnt-orange) 0%, var(--tennessee-deep) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .section-header p {
            font-size: 1.25rem;
            color: var(--graphite);
            line-height: 1.8;
        }

        /* Trust Indicators */
        .trust-section {
            background: white;
            padding: 4rem 2rem;
        }
        .trust-container { max-width: 1400px; margin: 0 auto; }
        .trust-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 3rem; text-align: center; }
        .trust-item { display: flex; flex-direction: column; align-items: center; gap: 1rem; }
        .trust-icon { width: 60px; height: 60px; background: linear-gradient(135deg, var(--burnt-orange) 0%, var(--cardinal-blue) 100%); border-radius: 12px; display: flex; align-items: center; justify-content: center; color: white; font-size: 1.5rem; }
        .trust-value { font-size: 2.5rem; font-weight: 900; color: var(--tennessee-deep); }
        .trust-label { color: var(--graphite); font-size: 0.875rem; }

        /* Features Section */
        .features-grid {
            max-width: 1400px;
            margin: 0 auto;
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            background: white;
            border-radius: 16px;
            padding: 2rem;
            transition: all 0.3s var(--transition);
            border: 1px solid rgba(0, 0, 0, 0.05);
            position: relative;
            overflow: hidden;
            opacity: 0;
            transform: translateY(30px);
            transition: opacity 0.6s var(--transition), transform 0.6s var(--transition);
        }

        .feature-card.is-visible {
            opacity: 1;
            transform: translateY(0);
        }

        .feature-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, var(--burnt-orange) 0%, var(--cardinal-blue) 100%);
            transform: scaleX(0);
            transition: transform 0.4s var(--transition);
            transform-origin: left;
        }

        .feature-card:hover::before {
            transform: scaleX(1);
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
        }
        .feature-icon { width: 60px; height: 60px; background: linear-gradient(135deg, rgba(191, 87, 0, 0.1) 0%, rgba(155, 203, 235, 0.1) 100%); border-radius: 12px; display: flex; align-items: center; justify-content: center; color: var(--burnt-orange); font-size: 1.5rem; margin-bottom: 1.5rem; }
        .feature-title { font-size: 1.5rem; font-weight: 700; color: var(--tennessee-deep); margin-bottom: 1rem; }
        .feature-description { color: var(--graphite); line-height: 1.8; margin-bottom: 1.5rem; }
        .feature-link { color: var(--burnt-orange); text-decoration: none; font-weight: 600; display: inline-flex; align-items: center; gap: 0.5rem; transition: gap 0.3s var(--transition); }
        .feature-link i { transition: transform 0.3s var(--transition); }
        .feature-link:hover i { transform: translateX(5px); }


        /* Other sections can be included here with minor changes if needed */
        /* For brevity, I'll keep the rest of the styles as they were, as they are solid */
        .integrations { padding: 6rem 2rem; background: white; }
        .integration-grid { max-width: 1200px; margin: 4rem auto 0; display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 2rem; }
        .integration-card { background: var(--pearl); border-radius: 12px; padding: 2rem; text-align: center; transition: all 0.3s var(--transition); border: 2px solid transparent; position: relative; }
        .integration-card.active { border-color: var(--burnt-orange); background: white; transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.05); }
        .integration-card.active::after { content: '✓ LIVE'; position: absolute; top: 1rem; right: 1rem; background: var(--success); color: white; padding: 0.25rem 0.5rem; border-radius: 4px; font-size: 0.75rem; font-weight: 700; }
        .integration-logo { height: 60px; margin-bottom: 1rem; display: flex; align-items: center; justify-content: center; font-size: 2.5rem; color: var(--tennessee-deep); }
        .integration-name { font-weight: 700; color: var(--tennessee-deep); margin-bottom: 0.5rem; }
        .integration-description { font-size: 0.875rem; color: var(--graphite); }
        /* Footer */
        footer { padding: 4rem 2rem 2rem; background: var(--tennessee-deep); color: white; }
        .footer-container { max-width: 1400px; margin: 0 auto; display: grid; grid-template-columns: 2fr 1fr 1fr 1fr; gap: 3rem; margin-bottom: 3rem; }
        .footer-brand h3 { font-size: 1.5rem; margin-bottom: 1rem; color: var(--cardinal-blue); }
        .footer-description { color: var(--platinum); line-height: 1.8; margin-bottom: 1.5rem; }
        .footer-socials { display: flex; gap: 1rem; }
        .social-link { width: 40px; height: 40px; background: rgba(255, 255, 255, 0.1); border-radius: 8px; display: flex; align-items: center; justify-content: center; color: white; text-decoration: none; transition: all 0.3s var(--transition); }
        .social-link:hover { background: var(--burnt-orange); transform: translateY(-2px); }
        .footer-section h4 { font-size: 1rem; margin-bottom: 1rem; color: var(--cardinal-blue); text-transform: uppercase; letter-spacing: 1px; }
        .footer-links { list-style: none; }
        .footer-links li { margin-bottom: 0.75rem; }
        .footer-links a { color: var(--platinum); text-decoration: none; transition: color 0.3s var(--transition); }
        .footer-links a:hover { color: var(--cardinal-blue); }
        .footer-bottom { padding-top: 2rem; border-top: 1px solid rgba(255, 255, 255, 0.1); text-align: center; color: var(--platinum); }


        /* Mobile Responsiveness */
        @media (max-width: 900px) {
            .hero-container {
                grid-template-columns: 1fr;
                text-align: center;
            }
            .hero-content {
                order: 2;
            }
            .hero-visual {
                order: 1;
                margin-bottom: 2rem;
            }
             .hero-description {
                margin-left: auto;
                margin-right: auto;
            }
            .hero-cta-group {
                justify-content: center;
            }
            .hero-content h1 {
                font-size: 2.8rem;
            }
        }
        @media (max-width: 768px) {
            .nav-links { display: none; }
            .footer-container { grid-template-columns: 1fr; text-align: center; }
            .footer-socials { justify-content: center; }
        }
    </style>
</head>
<body>
    <nav id="navbar">
        <div class="nav-container">
            <a href="#home" class="logo">
                <div class="logo-icon">B</div>
                Blaze Intelligence
            </a>
            <div class="nav-links">
                <a href="#features" class="nav-link">Features</a>
                <a href="#integrations" class="nav-link">Integrations</a>
                <a href="#about" class="nav-link">About</a>
                <a href="#demo" class="nav-cta">Try Live Demo</a>
            </div>
        </div>
    </nav>

    <main>
        <section class="hero" id="home">
            <div id="hero-canvas"></div>
            <div class="hero-container">
                <div class="hero-content">
                    <div class="tagline">Where Texas Grit Meets Silicon Valley Innovation</div>
                    <h1>Elite Sports Analytics Platform</h1>
                    <p class="hero-description">
                        Pioneer sports analytics through cutting-edge AI, real-time performance tracking, 
                        and predictive modeling. Transform raw data into championship decisions.
                    </p>
                    <div class="hero-cta-group">
                        <a href="#demo" class="btn-primary">
                            <i class="fas fa-chart-line"></i>
                            Try Live Dashboard
                        </a>
                        <a href="#features" class="btn-secondary">
                            <i class="fas fa-info-circle"></i>
                            Learn More
                        </a>
                    </div>
                </div>
                <div class="hero-visual">
                    <div class="live-stats-card">
                        <div class="live-badge">LIVE</div>
                        <h3>Real-Time Analytics</h3>
                        <div class="stat-grid">
                            <div class="stat-item">
                                <div class="stat-label"><i class="fas fa-bolt"></i>Perf. Index</div>
                                <div class="stat-value" id="perfIndex">94.6</div>
                                <div class="stat-change">+2.3%</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-label"><i class="fas fa-brain"></i>AI Confidence</div>
                                <div class="stat-value" id="aiConf">96.2%</div>
                                <div class="stat-change">GPT-4+</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-label"><i class="fas fa-baseball-ball"></i>MLB Games</div>
                                <div class="stat-value" id="mlbGames">15</div>
                                <div class="stat-change">Live StatsAPI</div>
                            </div>
                            <div class="stat-item">
                                <div class="stat-label"><i class="fas fa-tachometer-alt"></i>Latency</div>
                                <div class="stat-value" id="latency">87ms</div>
                                <div class="stat-change">Real-time</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <section class="trust-section">
            <div class="trust-container">
                <div class="trust-grid">
                    <div class="trust-item">
                        <div class="trust-icon"><i class="fas fa-code"></i></div>
                        <div class="trust-value">100%</div>
                        <div class="trust-label">TRANSPARENT DEVELOPMENT</div>
                    </div>
                    <div class="trust-item">
                        <div class="trust-icon"><i class="fas fa-plug"></i></div>
                        <div class="trust-value">4+</div>
                        <div class="trust-label">WORKING INTEGRATIONS</div>
                    </div>
                    <div class="trust-item">
                        <div class="trust-icon"><i class="fas fa-chart-line"></i></div>
                        <div class="trust-value">94.6%</div>
                        <div class="trust-label">PREDICTION ACCURACY</div>
                    </div>
                    <div class="trust-item">
                        <div class="trust-icon"><i class="fas fa-dollar-sign"></i></div>
                        <div class="trust-value">$497</div>
                        <div class="trust-label">SIMPLE PRICING</div>
                    </div>
                    <div class="trust-item">
                        <div class="trust-icon"><i class="fas fa-flag"></i></div>
                        <div class="trust-value">TX</div>
                        <div class="trust-label">TEXAS HERITAGE</div>
                    </div>
                </div>
            </div>
        </section>

        <section class="section features" id="features">
            <div class="section-header">
                <h2>Championship-Level Features</h2>
                <p>Everything you need to analyze, optimize, and dominate your competition</p>
            </div>
            <div class="features-grid">
                <div class="feature-card">
                    <div class="feature-icon"><i class="fas fa-bolt"></i></div>
                    <h3 class="feature-title">Real-Time Analytics</h3>
                    <p class="feature-description">Process millions of data points per second. Get insights as the action happens with our live MLB StatsAPI integration.</p>
                    <a href="#" class="feature-link">See it in action <i class="fas fa-arrow-right"></i></a>
                </div>
                <div class="feature-card">
                    <div class="feature-icon"><i class="fas fa-brain"></i></div>
                    <h3 class="feature-title">AI-Powered Predictions</h3>
                    <p class="feature-description">Machine learning models powered by OpenAI GPT-4 and Anthropic Claude predict outcomes with 94.6% accuracy.</p>
                    <a href="#" class="feature-link">Test AI analysis <i class="fas fa-arrow-right"></i></a>
                </div>
                <div class="feature-card">
                    <div class="feature-icon"><i class="fas fa-chart-pie"></i></div>
                    <h3 class="feature-title">Custom Dashboards</h3>
                    <p class="feature-description">Build personalized dashboards tailored to your team's specific KPIs with interactive D3.js & Three.js visualizations.</p>
                    <a href="#" class="feature-link">View dashboard demo <i class="fas fa-arrow-right"></i></a>
                </div>
            </div>
        </section>

        <section class="integrations" id="integrations">
            <div class="section-header">
                <h2>Actual Working Integrations</h2>
                <p>Real APIs, real data, real results - no smoke and mirrors</p>
            </div>
            <div class="integration-grid">
                <div class="integration-card active"><div class="integration-logo"><i class="fas fa-robot"></i></div><h4 class="integration-name">OpenAI GPT-4</h4><p class="integration-description">Live integration for team analysis and insights</p></div>
                <div class="integration-card active"><div class="integration-logo"><i class="fas fa-brain"></i></div><h4 class="integration-name">Anthropic Claude</h4><p class="integration-description">Advanced championship probability calculations</p></div>
                <div class="integration-card active"><div class="integration-logo"><i class="fas fa-credit-card"></i></div><h4 class="integration-name">Stripe Payments</h4><p class="integration-description">Secure subscription and payment processing</p></div>
                <div class="integration-card active"><div class="integration-logo"><i class="fas fa-baseball-ball"></i></div><h4 class="integration-name">MLB StatsAPI</h4><p class="integration-description">Real-time baseball data and statistics</p></div>
                <div class="integration-card"><div class="integration-logo"><i class="fas fa-football-ball"></i></div><h4 class="integration-name">NFL (Coming Soon)</h4><p class="integration-description">Next Gen Stats integration in development</p></div>
                <div class="integration-card"><div class="integration-logo"><i class="fas fa-basketball-ball"></i></div><h4 class="integration-name">NBA (Coming Soon)</h4><p class="integration-description">Advanced tracking data integration planned</p></div>
            </div>
        </section>
    </main>

    <footer>
        <div class="footer-container">
            <div class="footer-brand">
                <h3>Blaze Intelligence</h3>
                <p class="footer-description">Where Texas Grit Meets Silicon Valley Innovation. Building the future of sports analytics with integrity, transparency, and uncompromising quality.</p>
                <div class="footer-socials">
                    <a href="https://linkedin.com/in/john-humphrey-2033" class="social-link" target="_blank"><i class="fab fa-linkedin"></i></a>
                    <a href="mailto:ahump20@outlook.com" class="social-link"><i class="fas fa-envelope"></i></a>
                    <a href="tel:2102735538" class="social-link"><i class="fas fa-phone"></i></a>
                </div>
            </div>
            <div class="footer-section"><h4>Product</h4><ul class="footer-links"><li><a href="#features">Features</a></li><li><a href="#integrations">Integrations</a></li></ul></div>
            <div class="footer-section"><h4>Company</h4><ul class="footer-links"><li><a href="#about">About</a></li><li><a href="#">Contact</a></li></ul></div>
            <div class="footer-section"><h4>Contact</h4><ul class="footer-links"><li>Austin Humphrey</li><li>Founder & CEO</li><li><a href="mailto:ahump20@outlook.com">ahump20@outlook.com</a></li><li><a href="tel:2102735538">(210) 273-5538</a></li></ul></div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2024 Blaze Intelligence. Built with integrity in Austin, Texas. Intelligence. Integrity. Innovation.</p>
        </div>
    </footer>

    <script type="module">
        import * as THREE from 'https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.module.js';

        // --- THREE.JS HERO BACKGROUND ---
        let scene, camera, renderer, points;
        let mouse = new THREE.Vector2(-100, -100);

        function initThree() {
            const container = document.getElementById('hero-canvas');
            if (!container) return;

            // Scene
            scene = new THREE.Scene();

            // Camera
            camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 0.1, 1000);
            camera.position.z = 2.5;

            // Renderer
            renderer = new THREE.WebGLRenderer({ antialias: true, alpha: true });
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
            container.appendChild(renderer.domElement);
            
            // Particles
            const count = 5000;
            const positions = new Float32Array(count * 3);
            const colors = new Float32Array(count * 3);
            const sizes = new Float32Array(count);
            
            const colorBurntOrange = new THREE.Color(0xBF5700);
            const colorCardinalBlue = new THREE.Color(0x9BCBEB);

            for(let i = 0; i < count; i++) {
                let i3 = i * 3;
                positions[i3 + 0] = (Math.random() - 0.5) * 10;
                positions[i3 + 1] = (Math.random() - 0.5) * 10;
                positions[i3 + 2] = (Math.random() - 0.5) * 5;
                
                const mixedColor = Math.random() > 0.5 ? colorBurntOrange.clone() : colorCardinalBlue.clone();
                colors[i3 + 0] = mixedColor.r;
                colors[i3 + 1] = mixedColor.g;
                colors[i3 + 2] = mixedColor.b;
                
                sizes[i] = Math.random() * 0.1;
            }

            const geometry = new THREE.BufferGeometry();
            geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
            geometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));
            
            const material = new THREE.PointsMaterial({
                size: 0.02,
                vertexColors: true,
                blending: THREE.AdditiveBlending,
                transparent: true,
                depthWrite: false
            });

            points = new THREE.Points(geometry, material);
            scene.add(points);

            // Event Listeners
            window.addEventListener('resize', onWindowResize);
            document.addEventListener('mousemove', onMouseMove);
            
            animate();
        }

        function onWindowResize() {
            camera.aspect = window.innerWidth / window.innerHeight;
            camera.updateProjectionMatrix();
            renderer.setSize(window.innerWidth, window.innerHeight);
            renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        }

        function onMouseMove(event) {
            mouse.x = (event.clientX / window.innerWidth) * 2 - 1;
            mouse.y = -(event.clientY / window.innerHeight) * 2 + 1;
        }

        const clock = new THREE.Clock();
        function animate() {
            const elapsedTime = clock.getElapsedTime();
            
            // Animate points
            points.rotation.y = elapsedTime * 0.05;
            points.rotation.x = elapsedTime * 0.02;

            renderer.render(scene, camera);
            requestAnimationFrame(animate);
        }
        
        initThree();


        // --- SCROLL ANIMATIONS ---
        const animatedElements = document.querySelectorAll('.feature-card');
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('is-visible');
                    observer.unobserve(entry.target);
                }
            });
        }, {
            threshold: 0.2
        });
        animatedElements.forEach(el => observer.observe(el));


        // --- ORIGINAL SCRIPT FUNCTIONALITY ---
        // Navbar scroll effect
        window.addEventListener('scroll', function() {
            const navbar = document.getElementById('navbar');
            if (window.scrollY > 50) {
                navbar.style.boxShadow = '0 2px 20px rgba(0, 0, 0, 0.1)';
            } else {
                navbar.style.boxShadow = 'none';
            }
        });

        // Live stats animation
        function animateStats() {
            const stats = [
                { id: 'perfIndex', min: 92, max: 97, fixed: 1 },
                { id: 'aiConf', min: 94, max: 98, fixed: 1, suffix: '%' },
                { id: 'mlbGames', min: 10, max: 20, fixed: 0 },
                { id: 'latency', min: 75, max: 100, fixed: 0, suffix: 'ms' }
            ];

            stats.forEach(stat => {
                const element = document.getElementById(stat.id);
                if (element) {
                    const target = Math.random() * (stat.max - stat.min) + stat.min;
                    element.innerText = target.toFixed(stat.fixed) + (stat.suffix || '');
                }
            });
        }
        setInterval(animateStats, 3000);
        animateStats(); // initial call
    </script>
</body>
</html>
